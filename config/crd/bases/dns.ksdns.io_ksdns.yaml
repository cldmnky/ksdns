---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: ksdns.dns.ksdns.io
spec:
  group: dns.ksdns.io
  names:
    kind: Ksdns
    listKind: KsdnsList
    plural: ksdns
    singular: ksdns
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Ksdns is the Schema for the ksdns API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KsdnsSpec defines the desired state of Ksdns
            properties:
              zones:
                items:
                  properties:
                    origin:
                      type: string
                    records:
                      items:
                        properties:
                          data:
                            type: string
                          name:
                            type: string
                          port:
                            description: required if SRV
                            type: integer
                          priority:
                            description: required if SRV
                            type: integer
                          text:
                            description: Required if TXT
                            type: string
                          ttl:
                            default: 30
                            description: optional default is 30
                            maximum: 2147483647
                            minimum: 0
                            type: integer
                          type:
                            enum:
                            - A
                            - CNAME
                            - SRV
                            - TXT
                            type: string
                          weight:
                            description: required if SRV
                            type: integer
                        type: object
                      type: array
                  type: object
                type: array
            type: object
          status:
            description: KsdnsStatus defines the observed state of Ksdns
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
